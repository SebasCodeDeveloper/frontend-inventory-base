/**
* Configuración del entorno de la aplicación.
* En este archivo se definen las variables de entorno que se utilizan en la aplicación.
* En este caso, se define la URL del backend (apiUrl) que se utilizará para realizar las solicitudes HTTP.
*/
export const environment = {
    production: false,
    // Ajusta a la URL de backend (host y puerto)
apiUrl: 'http://localhost:8081/api'
};